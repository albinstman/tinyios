# Personal Claude Code settings (shared settings.json is committed)
.claude/settings.local.json

# Compiled binaries from `go build`
/tinyios
/node
/gendocs

# Go build/test output
*.exe
*.out
*.test

# App install scratch (downloaded zips / extracted .app bundles, named per device serial)
*.zip
